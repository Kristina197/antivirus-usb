#!/bin/bash

echo "🐳 Запуск USB Antivirus Scanner в Docker..."
echo "📍 С русской локализацией и доступом к USB устройствам"

# Разрешаем X11 подключения
xhost +local:docker

# Запускаем контейнер
docker-compose up --build

# Отзываем разрешение X11
xhost -local:docker
